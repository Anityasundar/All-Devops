apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-deployment2
  namespace: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: portfolio
  template:
    metadata:
      labels:
        app: portfolio
    spec:
      containers:
        - name: portfolio
          image: chinuvalentine/babe:latest
          ports:
            - containerPort: 3000

